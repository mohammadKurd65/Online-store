import { useAuth } from "../hooks/useAuth";
import { rolePermissions } from "../constants/rolePermissions";

export default function HasPermission({ permission, children }) {
const { role } = useAuth();
const userPermissions = rolePermissions[role] || [];

return userPermissions.includes(permission) ? <>{children}</> : null;
}
// filepath: e:\Online store\online-stora